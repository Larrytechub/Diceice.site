{
  "name": "diceice-site",
  "version": "0.1.0",
  "private": true,
  "description": "SmartCharts bundles and localization assets for Diceice.site.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node scripts/check-assets.mjs",
    "build": "node scripts/build.mjs",
    "clean": "node scripts/clean.mjs"
  }
}
